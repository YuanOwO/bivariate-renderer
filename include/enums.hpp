#pragma once

enum OPERATOR {
    OP_PLUS,
    OP_MINUS,
    OP_MULTIPLY,
    OP_DIVIDE,
    OP_EXPONENT,
};

enum IMAGE_TYPE {
    IMAGE_PNG,
    IMAGE_BMP,
    IMAGE_TGA,
    IMAGE_HDR,
    IMAGE_JPG,
    IMAGE_UNKNOWN,
};
