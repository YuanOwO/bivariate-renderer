/* 1. 變數賦值：直接以 識別碼 = 數值 結尾 */
a = ++--+-1;
b = 6+18;
c = a + b*2^2 + phi ;

/* 2. 函數定義：名稱(參數) = 運算式 */
f(x, y) = a * x + y^2 + cos(3*x*y);

g(x, y) = cos(f(x, y)) + 2*sin(x) - 3*y;

/* 3. 渲染區塊：用關鍵字 plot 包起來 */
plot( g(x, y), "output.jpg");
